Facade is a structural design pattern that provides a simplified (but limited) interface to a complex system of classes, library or framework. It hides the system's complexities by exposing only the essential features, reducing dependencies between clients and subsystems.
